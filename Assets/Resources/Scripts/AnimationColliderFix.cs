using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AnimationColliderFix : MonoBehaviour
{
    // Start is called before the first frame update
    public float offset;
    public bool isAttacking;
}
